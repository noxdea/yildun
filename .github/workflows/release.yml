name: Release
on:
  push:
    tags:
      - 'v*'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions: {}
jobs:
  publish:
    name: Publish gem
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write # Required to create the GitHub release.
      id-token: write # Required for RubyGems trusted publishing.
    environment: release
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: ruby/setup-ruby@bec3f19a76460dbe12f60def7d1a77585f07516c # v1.322.0
        with:
          ruby-version: '3.4'
      - name: Verify tag and version
        run: test "${GITHUB_REF_NAME}" = "v$(ruby -Ilib -ryildun/version -e 'print Yildun::VERSION')"
      - name: Test and build
        run: |
          bundle install
          bundle exec rake test
          bundle exec rbs -I sig validate
          gem build --strict yildun.gemspec --output /tmp/yildun.gem
      - uses: rubygems/configure-rubygems-credentials@dc5a8d8553e6ee01fc26761a49e99e733d17954a # v2.1.0
      - name: Publish through RubyGems trusted publishing
        run: gem push /tmp/yildun.gem
      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          version="${GITHUB_REF_NAME#v}"
          awk -v heading="## $version" '
            $0 == heading || index($0, heading " ") == 1 { found = 1; next }
            found && /^## / { exit }
            found { print }
            END { if (!found) exit 1 }
          ' CHANGELOG.md > "$RUNNER_TEMP/release-notes.md"
          grep -q '[^[:space:]]' "$RUNNER_TEMP/release-notes.md"
          gh release create "$GITHUB_REF_NAME" --verify-tag --title "$GITHUB_REF_NAME" --notes-file "$RUNNER_TEMP/release-notes.md"
